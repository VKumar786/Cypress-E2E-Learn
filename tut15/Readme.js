/*
    TODO: Web Automation | Hooks & Tags
    ? There are 4 type of hooks are avaiable
    ? Hooks are used to pre-execute some command
        * 1. before - it will be executed before all it block
        * 2. after - it will be executed after all it block
        * 3. beforeEach - it will be execcute before each it block
        * 4. afterEach - it will be execute after each it block
    ? There are some it block
        * it.skip
        * it.only
*/
