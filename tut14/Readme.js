/*
    TODO: Interacting with Elements | Handling File Upload
    ? Method
        * .attachFile({filePath, fileName})
        * .attachFile(filePath, { subjectType: 'drag-n-drop'})
        * .attachFile([filePath, filePath])
        * .find('#', {includeShadowDom: true})
    */
